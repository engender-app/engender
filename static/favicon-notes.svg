<!-- The disguised tab icon (F24). Deliberately dull, and the same book glyph
     the Settings preview shows, so the tab matches what the toggle promised.
     Fixed colours rather than palette tokens: a favicon gets no CSS.

     The path is a copy of `book` in src/lib/components/icons.ts and
     tests/favicon.test.ts is what keeps the two the same drawing. It earned
     its keep in phase 5 ticket 31: the optical-centring sweep moved `book`
     0.62 of a unit up, and this file was the only other place that path
     existed. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><rect width="100" height="100" rx="24" fill="#8A94A6"/><g transform="translate(14 14) scale(3)" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4.11 18.88A2.5 2.5 0 0 1 6.61 16.38H20.11V1.38H6.61A2.5 2.5 0 0 0 4.11 3.88z"/><path d="M20.11 16.38v5H6.61a2.5 2.5 0 0 1 0-5"/></g></svg>
